---
import type { SiteLink } from "../lib/site";

interface Props {
  leftLink?: SiteLink;
}

const {
  leftLink = { href: "/", label: "BACK" },
} = Astro.props as Props;
---

<header class="page-topbar">
  <a class="pill-ink" href={leftLink.href}>BACK</a>
</header>
